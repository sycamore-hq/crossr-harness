# Pins consumed by harness-bootstrap / sync-skills after first published tags.
# Not a third tracker — features.json remains the work log.
# Replace <tag> with real tags at split-08 dogfood. No git submodules.

skills = "<tag>"
loops  = "<tag>"
